# remap prefix to `
#set -g prefix `
#unbind C-b
#bind ` send-prefix

# help with Shft PgUp scrolling
# set -g terminal-overrides 'xterm*:smcup@:rmcup@'

# set 256 color
#set -g default-terminal "screen-256color"

# browse urls
#unbind u
#bind-key u capture-pane \; save-buffer /tmp/tmux-buffer \; run-shell "$TERMINAL -e 'cat /tmp/tmux-buffer | urlview'"


# set statusbar background to black
#set -g status-bg black

# set the forground text to white
#set -g status-fg white

# set active window to highlight red
#set-window-option -g window-status-current-bg red

# set session name to green
#set -g status-left '#[fg=green]#H'

# force a reload of the config file
#unbind r
#bind r source-file ~/.tmux.conf

# bind 'a' to prefix for nested sessions
#unbind a
#bind-key a send-prefix

# start window numbering at 1
#set -g base-index 1

# turn off window sizing
#setw -g aggressive-resize on

# Other
et-option -g mouse-select-pane off
set-option -g mouse-resize-pane off
set-option -g mouse-select-window off
set-window-option -g mode-mouse on
